import{cp,mkdir,readFile,rm,writeFile}from"node:fs/promises";import{dirname,join}from"node:path";import{fileURLToPath}from"node:url";const root=dirname(fileURLToPath(import.meta.url)),project=dirname(root),dist=join(root,"dist");await rm(dist,{recursive:true,force:true});for(const target of["chromium","firefox"]){const out=join(dist,target);await mkdir(join(out,"icons"),{recursive:true});for(const file of["background.js","content.js","popup.html","popup.css","popup.js"])await cp(join(root,"src",file),join(out,file));await writeFile(join(out,"manifest.json"),await readFile(join(root,`manifest.${target}.json`),"utf8"));for(const size of[32,128])await cp(join(project,"src-tauri","icons",size===32?"32x32.png":"128x128.png"),join(out,"icons",`icon-${size}.png`))}
